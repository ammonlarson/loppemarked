DB_HOST=localhost
DB_PORT=5433
DB_NAME=loppemarked
DB_USER=loppemarked
DB_PASSWORD=changeme123

# Shared-db mode (deployed runtime only): when set, the API ignores the
# dedicated DB_* vars above and builds its connection from this Secrets Manager
# secret payload (host, port, database, username, password). Leave unset locally.
# DB_SECRET_ID=rds/shared/loppemarked_staging
